_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
.bundle/

# Bundler's gem directory only. Anchored with a leading slash so it cannot
# also swallow assets/js/vendor/, which holds libraries the site must ship.
/vendor/
